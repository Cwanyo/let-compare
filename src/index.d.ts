declare module "let-compare" {
  /**
   * Deep compare both values.
   *
   * @param {*} a
   * @param {*} b
   * @returns {boolean} Returns `true` if both values are same, else `false`.
   */
  export function letCompare(a: any, b: any): boolean;
}
